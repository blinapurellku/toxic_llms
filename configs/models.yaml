model:
  name: bert-base-uncased
  max_seq_length: 128
  dropout: 0.1

training:
  batch_size: 32
  learning_rate: 2e-5
  num_epochs: 5
  save_path: ./models/

data:
  train_file: ./data/train.csv
  val_file: ./data/val.csv
